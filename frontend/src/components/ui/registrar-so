


"use client";

import { useState } from "react";

import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Button } from "@/components/ui/button";
import SuccessCard from "@/components/ui/modalCriarSenha";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export default function CriarSenha({ onClose, onContinue }) {
    return (
        <section className="flex justify-center items-center lg:h-[calc(100vh-80px)]">

            {/* <Card
                className="relative flex-1 flex flex-col rounded-2xl p-8 gap-8">
                <CardHeader className="p-0">
                    <div className="flex flex-col gap-3 self-stretch">
                        <div className="flex items-center justify-between">
                            <CardTitle className="text-2xl font-medium text-primary">
                                bbbbbbbbbbbbbbbbb
                            </CardTitle>
                        </div>
                        <CardDescription className="text-base font-normal max-w-2x">
                            
                            aaaaaaaaaaaaaaaaaaaaaa
                        </CardDescription>
                    </div>
                </CardHeader>

                <CardContent className="flex flex-col flex-1 gap-8 p-0">

                    <Separator orientation="horizontal" />

                    <ul className="flex flex-col gap-4 flex-1">    
                            <li
                                className="flex items-center gap-3 text-base font-normal text-muted-foreground">
                                <Check className="size-4 text-primary shrink-0" />
                                
                            </li>
                    </ul>

                    <Button
                        className="w-full h-12 cursor-pointer">
                        Comece já
                    </Button>

                </CardContent>
            </Card>
 */}
            <Card className="relative w-150 flex flex-col rounded-2xl p-8 gap-8">
                <CardHeader>
                    {/* ICON */}
                    <div className="flex justify-center">
                        <img src="/logo.svg" className="w-15 h-15"></img>
                    </div>

                    <div className="space-y-1 flex flex-col items-center justify-center">
                        <h2 className="text-2xl font-semibold">
                            Criação da Senha
                        </h2>
                        <p className="text-md text-gray-500 font-medium">
                            Crie uma senha para acessar sua conta.
                        </p>
                    </div>
                </CardHeader>

                <CardContent>
                    <form>
                        <div className="flex flex-col gap-8">
                            <div className="grid gap-3">
                                <Label className="font-medium text-muted-foreground">Senha</Label>
                                <Input className="h-9" type="password" />
 
                                <Label className="font-medium text-muted-foreground mt-2">Confirmar Senha</Label>
                                <Input className="h-9" type="password" />

                            </div>
                        </div>
                        {/* BUTTON LOGIN */}
                        <div className="w-full space-y-4 mt-8">
                            <Button onClick={() => setOpen(true)} className="w-full h-10 bg-slate-900 hover:bg-slate-800 text-white text-md font-medium rounded-lg">
                                Criar
                            </Button>

                            {/* {open && (
                            <div className="fixed inset-0 bg-black/40 flex items-center justify-center z-50">
                                <SuccessCard
                                    onClose={() => setOpen(false)}
                                    onContinue={() => setOpen(false)}
                                />
                            </div>
                        )} */}
                        </div>
                    </form>

                </CardContent>

            </Card>

            {/* <div className="w-full max-w-[320px] bg-white rounded-2xl border shadow-lg p-6 relative flex flex-col gap-4 text-center">

                
                <button
                    onClick={onClose}
                    className="absolute top-4 right-4 text-gray-500 hover:text-black"
                >
                    ✕
                </button>



                <div className="space-y-1">
                    <h2 className="text-lg font-semibold">
                        Conta criada com sucesso!
                    </h2>
                    <p className="text-sm text-gray-500">
                        Tudo pronto para acompanhar sua produção em tempo real.
                    </p>
                </div>

                <div className="w-full max-w-sm space-y-4">
                    <Button onClick={() => setOpen(true)} className="w-full h-9 bg-slate-900 hover:bg-slate-800 text-white text-sm font-medium rounded-lg">
                        Criar
                    </Button>

                    {open && (
                        <div className="fixed inset-0 bg-black/40 flex items-center justify-center z-50">
                            <SuccessCard
                                onClose={() => setOpen(false)}
                                onContinue={() => setOpen(false)}
                            />
                        </div>
                    )}
                </div>

            </div> */}






        </section>
    );
}
